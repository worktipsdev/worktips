#define DEF_WORKTIPS_VERSION_MAJOR 2
#define DEF_WORKTIPS_VERSION_MINOR 1
#define DEF_WORKTIPS_VERSION_PATCH 4

#define WORKTIPS_STRINGIFY2(val) #val
#define WORKTIPS_STRINGIFY(val) WORKTIPS_STRINGIFY2(val)
#define DEF_WORKTIPS_VERSION WORKTIPS_STRINGIFY(DEF_WORKTIPS_VERSION_MAJOR) "." WORKTIPS_STRINGIFY(DEF_WORKTIPS_VERSION_MINOR) "." WORKTIPS_STRINGIFY(DEF_WORKTIPS_VERSION_PATCH)

#define DEF_WORKTIPS_VERSION_TAG "@VERSIONTAG@"
#define DEF_WORKTIPS_RELEASE_NAME "Reborn"
#define DEF_WORKTIPS_VERSION_FULL DEF_WORKTIPS_VERSION "-" DEF_WORKTIPS_VERSION_TAG

#include "version.h"

const int WORKTIPS_VERSION_MAJOR = DEF_WORKTIPS_VERSION_MAJOR;
const int WORKTIPS_VERSION_MINOR = DEF_WORKTIPS_VERSION_MINOR;
const int WORKTIPS_VERSION_PATCH = DEF_WORKTIPS_VERSION_PATCH;

const char* const WORKTIPS_VERSION_TAG = DEF_WORKTIPS_VERSION_TAG;
const char* const WORKTIPS_VERSION = DEF_WORKTIPS_VERSION;
const char* const WORKTIPS_RELEASE_NAME = DEF_WORKTIPS_RELEASE_NAME;
const char* const WORKTIPS_VERSION_FULL = DEF_WORKTIPS_VERSION_FULL;
